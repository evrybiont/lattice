- step = 0

- while products[step]
  .row
    - 4.times do |i|
      - if products[i + step].nil?
        - break
      = render partial: 'product', locals: {product: products[i + step]}
    - step += 4
